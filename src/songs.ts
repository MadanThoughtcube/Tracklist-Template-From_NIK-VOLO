export interface Song {
    //figure out if you can add images into a interface
    index: string;
    imageURL: string;
    name: string;
    artist: string;
    timestamp: string;
    status: Boolean;
}

